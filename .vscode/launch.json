{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run DoomLike",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/DoomLike",
      "preLaunchTask": "Build DoomLike (make)",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb"
    }
  ]
}
